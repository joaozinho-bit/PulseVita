# Backend - PulseVita

## Iniciar o projeto

Abrir o terminal na pasta do projeto e executar:

```bash
cd backend/pulsevita
./mvnw clean spring-boot:run
```

O backend ficará disponível após o arranque do Spring Boot.